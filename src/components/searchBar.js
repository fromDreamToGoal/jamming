import React, { useState } from "react";

function SearchBar () {
    return (
        <form>
            <input type="text"></input>
            <button>Search</button>
       </form>
    );
};

export default SearchBar;